{
  "name": "withastro-docs-mock",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "bench:seed": "node scripts/seed-pages.mjs 1000",
    "bench:seed:mdx": "MDX=1 node scripts/seed-pages.mjs 1000",
    "bench:cold": "FASTMD_LOG=json /usr/bin/time -p pnpm -s build",
    "bench:warm": "FASTMD_LOG=json /usr/bin/time -p pnpm -s build"
  },
  "dependencies": {
    "astro": "5.13.5"
  },
  "devDependencies": {
    "@astrojs/mdx": "^4.0.0",
    "rehype-autolink-headings": "^7.0.0",
    "rehype-slug": "^6.0.0",
    "remark-gfm": "^4.0.0"
  }
}
